import { type ITopic } from '@/entities/topic';

export const configJSON: ITopic = {
  value: 'json',
  name: 'JSON',
  content: {
    introduction:
      'JSON (JavaScript Object Notation) — независимый от языка текстовый формат для представления данных. Спецификация задокументирована в стандарте RFC 4627.',
    sections: [
      {
        title: 'Что такое JSON и для чего используется',
        content:
          'JSON — универсальный формат обмена данными между клиентом и сервером.\n' +
          'Полученная строка json называется JSON-форматированным или сериализованным объектом.\n' +
          'Поддерживаемые типы:\n' +
          '• Объекты {}\n' +
          '• Массивы []\n' +
          '• Строки, числа, логические значения (true/false) и null',
        addition: 'JSON является языково-независимой спецификацией данных.',
        examples:
          '// Пример JSON-объекта\n' +
          '{\n' +
          '  "name": "Иван",\n' +
          '  "age": 30,\n' +
          '  "isAdmin": true,\n' +
          '  "skills": ["JavaScript", "React"],\n' +
          '  "address": {\n' +
          '    "city": "Москва",\n' +
          '    "street": "Тверская"\n' +
          '  }\n' +
          '}',
      },
      {
        title: 'Правила и ограничения формата',
        content:
          'JSON имеет строгий синтаксис:\n' +
          '• Все строки и имена свойств обязательно в двойных кавычках (")\n' +
          '• Одинарные и обратные кавычки недопустимы\n' +
          '• Формат игнорирует функции (методы), символьные ключи/значения (Symbol) и undefined\n' +
          '• Запрет циклических ссылок (структура не должна содержать ссылок на саму себя)\n' +
          '• Не поддерживаются комментарии, конструкторы (new Date()), висячие запятые',
        addition:
          'Ключи объектов всегда должны быть в кавычках — это главное отличие от JS-литерала.',
        examples:
          '// ✅ Правильный JSON\n' +
          '{"name": "Иван", "age": 30}\n\n' +
          '// ❌ Ошибки:\n' +
          "// {name: 'Иван'} — ключ без кавычек\n" +
          "// {'name': 'Иван'} — одинарные кавычки\n" +
          '// {"sayHi": function() {}} — функции не поддерживаются\n' +
          '// {"id": Symbol("id")} — Symbol не поддерживается\n' +
          '// {"value": undefined} — undefined не поддерживается\n' +
          '// {"arr": [1, 2,]} — висячая запятая запрещена',
      },
    ],
  },
};
