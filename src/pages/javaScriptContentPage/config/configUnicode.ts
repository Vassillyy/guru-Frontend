import { type ITopic } from '@/entities/topic';

export const configUnicode: ITopic = {
  value: 'unicode',
  name: 'Юникод',
  content: {
    introduction:
      'Юникод — это стандарт кодирования символов. В JavaScript строки основаны на Юникоде: каждый символ представляет собой последовательность от 1 до 4 байтов.',
    sections: [
      {
        title: 'Нотации для символов',
        content:
          'JavaScript позволяет вставить символ, указав его шестнадцатеричный код одной из трёх нотаций.',
        addition:
          '• \\xXX — 2 шестнадцатеричные цифры (диапазон: 00–FF)\n' +
          '• \\uXXXX — 4 шестнадцатеричные цифры (диапазон: 0000–FFFF)\n' +
          '• \\u{X…XXXXXX} — от 1 до 6 шестнадцатеричных цифр (диапазон: 0–10FFFF)',
        examples:
          '// \\xXX — символы с кодом 0–255\n' +
          'console.log("\\x7A"); // z\n' +
          'console.log("\\xA9"); // ©\n\n' +
          '// \\uXXXX — символы с кодом 0–65535\n' +
          'console.log("\\u00A9"); // ©\n' +
          'console.log("\\u044F"); // я\n' +
          'console.log("\\u2191"); // ↑\n\n' +
          '// \\u{} — любые символы Юникода\n' +
          'console.log("\\u{20331}"); // 佫\n' +
          'console.log("\\u{1F60D}"); // 😍',
      },
      {
        title: 'Суррогатные пары',
        content:
          'Символы с кодом выше U+FFFF кодируются парой 2-байтовых символов — суррогатной парой. Это нужно, так как UTF-16 в JavaScript предусматривает только 2 байта на символ.',
        addition:
          '• length показывает 2 для таких символов\n' +
          '• Диапазон первой части: 0xD800–0xDBFF\n' +
          '• Диапазон второй части: 0xDC00–0xDFFF\n' +
          '• Части бессмысленны по отдельности',
        examples:
          'console.log("𝒳".length); // 2\n' +
          'console.log("😂".length); // 2\n\n' +
          '// charCodeAt vs codePointAt\n' +
          'console.log("𝒳".charCodeAt(0).toString(16)); // d835\n' +
          'console.log("𝒳".codePointAt(0).toString(16)); // 1d4b3\n\n' +
          '// Опасность: разделение строки может сломать суррогатную пару\n' +
          '"hi 😂".slice(0, 4); // "hi �" (получен битый символ)',
      },
      {
        title: 'Диакритические знаки и нормализация',
        content:
          'Символы с надстрочными/подстрочными знаками можно составить из основного символа и одного или нескольких символов-меток.',
        addition:
          '• Метки добавляются после основного символа\n' +
          '• Визуально одинаковые строки могут иметь разный Юникод\n' +
          '• Для приведения к единому виду используется нормализация (метод normalize)',
        examples:
          '// Составные символы\n' +
          'console.log("S\\u0307"); // Ṡ\n' +
          'console.log("S\\u0307\\u0323"); // Ṩ\n\n' +
          '// Проблема: разные коды, но одинаковый вид\n' +
          'let s1 = "S\\u0307\\u0323";\n' +
          'let s2 = "S\\u0323\\u0307";\n' +
          'console.log(s1 == s2); // false\n\n' +
          '// Решение: нормализация\n' +
          'console.log(s1.normalize() == s2.normalize()); // true\n' +
          '// Метод normalize() может "схлопнуть" последовательность в один символ\n' +
          'console.log("S\\u0307\\u0323".normalize().length); // 1',
      },
    ],
  },
};
