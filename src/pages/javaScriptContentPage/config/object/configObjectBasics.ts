import { type ITopic } from '@/entities/topic';

export const configObjectBasics: ITopic = {
  value: 'object-basics',
  name: 'Основы объектов',
  content: {
    introduction:
      'Объекты в JavaScript — это ассоциативные массивы с дополнительными возможностями. Они позволяют хранить коллекции данных и сложные структуры, где каждое значение имеет уникальный ключ (имя свойства).',
    sections: [
      {
        title: 'Создание объектов: литерал, конструктор и Object.create()',
        content:
          'Три способа создания объектов:\n' +
          '- Литерал: const obj = { ключ: значение }\n' +
          '- Конструктор: const obj = new Object()\n' +
          '- Метод: const obj = Object.create(прототип)',
        addition:
          'Литерал объекта короче, читаемее и быстрее, чем конструктор Object, потому что при литерале объект создается сразу в виде хеш-таблицы, а конструктор вызывает функцию. Object.create() используется для явного указания прототипа.',
        examples:
          '// Литерал объекта (рекомендуется)\n' +
          'const user = {\n' +
          '  name: "Иван",\n' +
          '  age: 30,\n' +
          '  isAdmin: true\n' +
          '};\n\n' +
          '// Конструктор Object (эквивалентно)\n' +
          'const user2 = new Object();\n' +
          'user2.name = "Иван";\n' +
          'user2.age = 30;\n\n' +
          '// Object.create() с явным прототипом\n' +
          'const animal = {\n' +
          '  speak() {\n' +
          '    console.log(`${this.name} издает звук`);\n' +
          '  }\n' +
          '};\n\n' +
          'const dog = Object.create(animal);\n' +
          'dog.name = "Бобик";\n' +
          'dog.speak(); // "Бобик издает звук"',
      },
      {
        title: 'Свойства объекта: чтение, запись, удаление',
        content:
          'Свойства объекта — это пары "ключ: значение". Ключи (имена свойств) всегда строки или символы, значения могут быть любого типа.\n\n' +
          'Доступ к свойствам:\n' +
          '• Через точку: obj.property\n' +
          '• Квадратные скобки: obj["property"]\n\n' +
          'Удаление свойств: delete obj.property',
        addition:
          'При использовании чтения через точку имя свойства должно быть валидным идентификатором (буквы, цифры, $, _, не может начинаться с цифры). Квадратные скобки позволяют использовать динамические ключи: ключи с пробелами, спецсимволами и числа.',
        examples:
          'const user = {\n' +
          '  name: "Иван",\n' +
          '  age: 30\n' +
          '};\n\n' +
          '// Чтение свойства\n' +
          'console.log(user.name); // "Иван"\n' +
          'console.log(user["age"]); // 30\n\n' +
          '// Запись свойства\n' +
          'user.age = 31;\n' +
          'user["city"] = "Москва";\n\n' +
          '// Динамический ключ\n' +
          'const key = "name";\n' +
          'console.log(user[key]); // "Иван"\n\n' +
          '// Ключи с пробелами и спецсимволами\n' +
          'const obj = {\n' +
          '  "first-name": "Иван",\n' +
          '  "age years": 30\n' +
          '};\n' +
          'console.log(obj["first-name"]); // "Иван"\n' +
          '// obj.first-name — ошибка!\n\n' +
          '// Удаление свойства\n' +
          'delete user.age;\n' +
          'console.log(user.age); // undefined',
      },
      {
        title: 'Вычисляемые свойства',
        content:
          'Вычисляемые свойства позволяют использовать выражение в квадратных скобках как ключ свойства (динамическое создание).\n\n' +
          'const obj = { [выражение]: значение }',
        addition:
          'Выражение в квадратных скобках вычисляется и результат преобразуется в строку (если это не Symbol).',
        examples:
          '// Вычисляемое свойство при создании\n' +
          'const key = "id";\n' +
          'const obj = {\n' +
          '  [key]: 123,\n' +
          '  [`${key}Name`]: "Пользователь"\n' +
          '};\n' +
          'console.log(obj.id); // 123\n' +
          'console.log(obj.idName); // "Пользователь"\n\n' +
          '// Symbol как вычисляемое свойство\n' +
          'const id = Symbol("id");\n' +
          'const entity = {\n' +
          '  [id]: 42,\n' +
          '  name: "Объект"\n' +
          '};\n' +
          'console.log(entity[id]); // 42',
      },
      {
        title: 'Порядок свойств в объекте',
        content:
          'Свойства упорядочены особым образом: свойства с целочисленными ключами сортируются по возрастанию, остальные располагаются в порядке создания.\n' +
          'Термин «целочисленное свойство» означает строку, которая может быть преобразована в целое число и обратно без изменений.',
        addition:
          'Нет никаких ограничений к именам свойств. Ключи автоматически преобразуются в строки, за исключением Symbol — они используются как есть.',
        examples:
          '// Числа как ключи (преобразуются в строки)\n' +
          'const obj = {\n' +
          '  1: "один",\n' +
          '  2: "два"\n' +
          '};\n' +
          'console.log(obj[1]); // "один"\n' +
          'console.log(obj["1"]); // "один" (эквивалентно)\n\n' +
          '// Спецсимволы в имени (только квадратные скобки)\n' +
          'const special = {\n' +
          '  "first-name": "Иван",\n' +
          '  "email@domain": "test@example.com"\n' +
          '};\n' +
          'console.log(special["first-name"]); // "Иван"\n\n' +
          '// Целочисленные ключи сортируются по возрастанию\n' +
          'const ordered = {\n' +
          '  3: "три",\n' +
          '  1: "один",\n' +
          '  2: "два"\n' +
          '};\n' +
          'console.log(Object.keys(ordered)); // ["1", "2", "3"] (отсортировано!)\n\n' +
          '// Нецелочисленные ключи — в порядке создания\n' +
          'const notOrdered = {\n' +
          '  b: "b",\n' +
          '  a: "a",\n' +
          '  c: "c"\n' +
          '};\n' +
          'console.log(Object.keys(notOrdered)); // ["b", "a", "c"] (порядок создания)',
      },
    ],
  },
};
