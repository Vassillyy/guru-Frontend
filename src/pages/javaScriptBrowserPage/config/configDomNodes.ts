import { type ITopic } from '@/entities/topic';

export const configDomNodes: ITopic = {
  value: 'dom-nodes',
  name: 'DOM-дерево',
  content: {
    introduction:
      'DOM (Document Object Model) — это программный интерфейс, который представляет HTML- или XML-документ в виде структуры, где каждый элемент является объектом. Эта структура называется "деревом узлов", и именно с её помощью JavaScript может взаимодействовать со страницей.',
    sections: [
      {
        title: 'Что такое DOM?',
        content:
          'Когда браузер загружает HTML-страницу, он парсит её код и на его основе создаёт DOM-дерево. Корнем этого дерева является объект document.\n' +
          '• Каждый HTML-тег становится "узлом-элементом".\n' +
          '• Вложенные теги становятся "детьми" родительского элемента.\n' +
          '• Текст внутри тега становится "текстовым узлом".',
        addition:
          'Пробелы и переводы строк в коде также создают текстовые узлы в DOM.',
        examples:
          '<!DOCTYPE HTML>\n' +
          '<html>\n' +
          '  <head>\n' +
          '    <title>О лосях</title>\n' +
          '  </head>\n' +
          '  <body>\n' +
          '    Правда о лосях.\n' +
          '  </body>\n' +
          '</html>\n\n' +
          '// DOM-дерево для этого документа:\n' +
          'document\n' +
          '  └── html (корневой элемент)\n' +
          '        ├── head\n' +
          '        │     └── title\n' +
          '        │           └── #text: "О лосях"\n' +
          '        └── body\n' +
          '              └── #text: "Правда о лосях."',
      },
      {
        title: 'Автоисправление',
        content:
          'Если браузер сталкивается с некорректно написанным HTML-кодом, он автоматически корректирует его при построении DOM.',
        addition:
          '• Браузер сам создаёт теги <html> и <body>, если их нет\n' +
          '• Браузер сам закрывает незакрытые теги\n' +
          '• Таблицы всегда содержат <tbody> (браузер добавляет его автоматически)\n' +
          '• Всё содержимое после </body> перемещается в конец body',
        examples:
          '// Некорректный HTML\n' +
          '<p>Привет\n' +
          '<li>Мама\n' +
          '<li>и\n' +
          '<li>Папа\n' +
          '// DOM будет нормальным, браузер сам закроет теги\n\n' +
          '// Таблица без tbody\n' +
          '<table id="table"><tr><td>1</td></tr></table>\n' +
          '// Браузер добавит <tbody> автоматически',
      },
      {
        title: 'Типы узлов',
        content:
          'DOM состоит из узлов разных типов. У каждого узла есть три важных свойства: nodeType (тип узла в виде числа), nodeName (имя узла) и nodeValue (значение узла).',
        addition:
          '1. document — корневой узел, "входная точка" в DOM. Представляет весь документ.\n' +
          '• nodeType = 9\n' +
          '• nodeName = "#document"\n' +
          '• nodeValue = null\n' +
          '2. Узлы-элементы — соответствуют HTML-тегам (<div>, <p>, <a>, <li> и т.д.). Формируют структуру документа и могут иметь потомков.\n' +
          '• nodeType = 1\n' +
          '• nodeName = "DIV" (имя тега в верхнем регистре)\n' +
          '• nodeValue = null\n' +
          '3. Текстовые узлы — содержат текст внутри элемента. Не могут иметь потомков, всегда находятся на самом нижнем уровне.\n' +
          '• nodeType = 3\n' +
          '• nodeName = "#text"\n' +
          '• nodeValue — сам текст (например, "Правда о лосях")\n' +
          '4. Узлы-комментарии — не влияют на визуальное отображение, но являются полноправной частью DOM и могут быть прочитаны через JavaScript.\n' +
          '• nodeType = 8\n' +
          '• nodeName = "#comment"\n' +
          '• nodeValue — содержимое комментария (например, " комментарий ")',
        examples:
          '<!DOCTYPE html>\n' +
          '<html lang="en">\n' +
          '<head>\n' +
          '  <meta charset="utf-8">\n' +
          '  <title>Типы узлов DOM</title>\n' +
          '</head>\n' +
          '<body>\n' +
          '  Правда о лосях.\n' +
          '  <div>Лось -- животное хитрое</div>\n' +
          '  <!-- комментарий -->\n\n' +
          '  <script>\n' +
          '    // Вывод всех узлов внутри body\n' +
          '    document.body.childNodes.forEach((node, index) => {\n' +
          '      console.log(`${index + 1}: тип ${node.nodeType}, имя "${node.nodeName}", значение:`, node.nodeValue);\n' +
          '    });\n' +
          '    // 1: тип 3, имя "#text", значение: "  Правда о лосях."\n' +
          '    // 2: тип 1, имя "DIV", значение: null\n' +
          '    // 3: тип 3, имя "#text", значение: (перевод строки плюс 2 пробела)\n' +
          '    // 4: тип 8, имя "#comment", значение: " комментарий "\n' +
          '    // 5: тип 3, имя "#text", значение: (перевод строки плюс 2 пробела)\n' +
          '    // 6: тип 1, имя "SCRIPT", значение: null\n' +
          '  </script>\n' +
          '</body>\n' +
          '</html>',
      },
    ],
  },
};
