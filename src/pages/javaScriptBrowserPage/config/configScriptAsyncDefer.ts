import { type ITopic } from '@/entities/topic';

export const configScriptAsyncDefer: ITopic = {
  value: 'script-async-defer',
  name: 'Атрибуты: async, defer',
  content: {
    introduction:
      'По умолчанию браузер блокирует построение DOM при встрече тега <script> — он должен сначала загрузить и выполнить скрипт. Это ведёт к двум проблемам: скрипты не видят DOM-элементы ниже себя, а объёмные скрипты блокируют отрисовку страницы. Атрибуты async и defer решают эти проблемы, позволяя управлять загрузкой и выполнением скриптов.',
    sections: [
      {
        title: 'Атрибут defer',
        content:
          'Атрибут defer сообщает браузеру продолжать обрабатывать страницу и загружать скрипт в фоновом режиме, а запустить его — только после того, как DOM-дерево будет полностью построено.\n' +
          '• Скрипты с defer никогда не блокируют страницу\n' +
          '• Всегда выполняются, когда DOM готов, но до события DOMContentLoaded\n' +
          '• Сохраняют порядок относительно друг друга, как обычные скрипты — даже если маленький скрипт загрузится первым, он будет ждать выполнения предыдущего',
        addition:
          'Атрибут defer предназначен только для внешних скриптов. Если в теге <script> нет src, defer игнорируется.',
        examples:
          '<p>...содержимое перед скриптом...</p>\n' +
          '<script defer src="long.js"></script>\n' +
          '<p>...содержимое после скрипта...</p>\n' +
          '<!-- Содержимое отобразится сразу, скрипт выполнится после построения DOM -->\n\n' +
          '<script>\n' +
          "  document.addEventListener('DOMContentLoaded', () => console.log('DOM готов'));\n" +
          '</script>\n' +
          '<script defer src="long.js"></script>\n' +
          '// DOMContentLoaded дождётся выполнения отложенного скрипта',
      },
      {
        title: 'Атрибут async',
        content:
          'Атрибут async делает скрипт полностью независимым:\n' +
          '• Страница не ждёт асинхронные скрипты — содержимое обрабатывается и отображается сразу\n' +
          '• DOMContentLoaded и асинхронные скрипты не ждут друг друга — событие может произойти как до, так и после async-скрипта\n' +
          '• Остальные скрипты не ждут async, и async не ждёт другие скрипты\n' +
          '• Порядок выполнения — по загрузке: какой скрипт загрузился первым, тот и выполняется первым',
        addition:
          'Атрибут async предназначен только для внешних скриптов. Если в теге <script> нет src, async игнорируется.',
        examples:
          '<p>...содержимое перед скриптами...</p>\n' +
          '<script async src="long.js"></script>\n' +
          '<script async src="small.js"></script>\n' +
          '<p>...содержимое после скриптов...</p>\n' +
          '<!-- Содержимое отображается сразу -->\n' +
          '<!-- small.js скорее всего загрузится и выполнится первым -->',
      },
      {
        title: 'Динамически загружаемые скрипты',
        content:
          'Скрипт можно добавить динамически, создав элемент script через JavaScript. Он начнёт загружаться сразу после добавления в документ.\n' +
          'По умолчанию динамически загружаемые скрипты ведут себя как async — они никого не ждут, и их никто не ждёт. Порядок выполнения определяется загрузкой: какой загрузился первым, тот и выполняется первым.',
        addition:
          'Чтобы переключить динамический скрипт на поведение defer (последовательное выполнение в порядке добавления в документ), нужно явно установить свойство async в false.',
        examples:
          '<script>\n' +
          '  document.addEventListener("DOMContentLoaded", () => {\n' +
          '    // Динамически загружаемый скрипт (async по умолчанию)\n' +
          '    let script = document.createElement("script");\n' +
          '    script.src = "/article/script-async-defer/long.js";\n' +
          '    document.body.append(script);\n\n' +
          '    // Принудительное отключение async — скрипт выполнится в порядке документа\n' +
          '    function loadScript(src) {\n' +
          '      let script = document.createElement("script");\n' +
          '      script.src = src;\n' +
          '      script.async = false;\n' +
          '      document.body.append(script);\n' +
          '    }\n' +
          '    loadScript("/article/script-async-defer/long.js");\n' +
          '    loadScript("/article/script-async-defer/small.js");\n' +
          '    // long.js выполнится первым, хотя small.js может загрузиться быстрее\n' +
          '  });\n' +
          '</script>',
      },
    ],
  },
};
